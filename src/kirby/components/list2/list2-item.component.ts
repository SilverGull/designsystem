import { ListItem } from './list-item';

/**
 * Using this interface the list component has a standard way of passing data to the list-items
 */
export interface List2ItemComponent {
    data: ListItem;
}
